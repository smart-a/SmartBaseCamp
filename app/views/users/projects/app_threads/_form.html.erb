<%= form_with(model: app_thread, local: true, class:'col-5') do |form| %>
  <% if @app_thread.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app_thread.errors.count, "error") %> prohibited this thread from being saved:</h2>

      <ul>
        <% @app_thread.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mt-5">
    <%= form.label 'Thread' %>
    <%= form.text_area :th_content, class:'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit 'Update', class:'btn btn-success' %>
  </div>
<% end %>
