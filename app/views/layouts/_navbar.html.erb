   <nav class="navbar navbar-expand-lg bg-dark navbar-dark sticky-top shadow">
      <div class="navbar-brand brand">
        <%= image_tag "logo.png", width:'60', height:'60' %>
        SmartBaseCamp
      </div>
      
      <button class="navbar-toggler " type="button" data-toggle="collapse" data-target="#navbarmenu" >
          <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarmenu">
        
        <% if @user_session.nil? %>
          <ul class="navbar-nav ml-auto">
            <li class="nav-item mr-2">
              <%= link_to 'Sign In', :users_login, class:"btn btn-sm btn-dark" %>
            </li>
            <li class="nav-item">
              <%= link_to 'Sign Up', :new_user, class:"btn btn-sm btn-dark" %>
            </li>
          </ul>
        <% else %>
          <ul class="navbar-nav">
              <li class="nav-item">
                <%= link_to 'My Profile', user_path(@user_session['id']) , class:"btn btn-sm btn-secondary" %>       
              </li>
            <% if @user_session['user_role']=='admin' %>
              <li class="nav-item">
                <a class="btn btn-sm btn-secondary" href="#">Admin Panel</a>
              </li>
            <% end %>
          </ul>
          <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <div class="nav-link mr-2">
                <i class="fa fa-user"></i>
                <%= @user_session['email'] %>
              </div>
            </li>
            <li class="nav-item">
              <%= link_to 'Logout', :users_logout, class:"btn btn-sm btn-danger" %>
            </li>
          </ul>
        <% end %>
      </div>
            
    </nav>