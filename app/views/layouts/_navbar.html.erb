   <nav class="navbar navbar-expand-lg bg-dark navbar-dark sticky-top shadow">
      <div class="navbar-brand brand">
        <%= image_tag "logo.png", width:'60', height:'60' %>
        SmartBaseCamp
      </div>
      
      <button class="navbar-toggler " type="button" data-toggle="collapse" data-target="#navbarmenu" >
          <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarmenu">
        <ul class="navbar-nav">
            <li class="nav-item">
              <a class="btn btn-sm btn-secondary", href="users/show?id=<%= @user_session['id'] %>">My Profile</a>          </li>
          <% if @user_session[:user_role]=='admin' %>
            <li class="nav-item">
              <a class="btn btn-sm btn-secondary" href="#">Admin Panel</a>
            </li>
          <% end %>
        </ul>

        <% if @user_session.nil? %>
          <ul class="navbar-nav ml-auto">
            <li class="nav-item mr-2">
              <a class="btn btn-success" href="#">Sign In</a>
            </li>
            <li class="nav-item">
              <a class="btn btn-success" href="#">Sign Up</a>
            </li>
          </ul>
        <% else %>
          <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <div class="nav-link mr-2">
                <i class="fa fa-user"></i>
                <%= @user_session[:email] %>
              </div>
            </li>
            <li class="nav-item">
              <a class="btn btn-sm btn-danger" href="#">Logout</a>
            </li>
          </ul>
        <% end %>
      </div>
            
    </nav>