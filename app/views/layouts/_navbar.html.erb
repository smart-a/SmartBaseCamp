   <nav class="navbar navbar-expand-lg bg-dark navbar-dark sticky-top shadow">
      <div class="navbar-brand brand">
        <%= image_tag "logo.png", width:'60', height:'60' %>
        SmartBaseCamp
      </div>
      
      <button class="navbar-toggler " type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation" >
          <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <%#= "#{logged_in?} CID: #{current_user[:id]} - PID #{params[:user_id]} " %>
        <% if logged_in? and current_user  %>
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to 'My Profile', user_path(current_user['id']) , class:"btn btn-sm btn-secondary" %>       
            </li>
            <% if current_user['user_role']=='admin' %>
              <li class="nav-item ml-2">
                <%= link_to 'Admin Panel', user_admin_index_path(current_user['id']) , class:"btn btn-sm btn-secondary" %>
              </li>
            <% end %>
          </ul>
          <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <div class="nav-link mr-2">
                <i class="fa fa-user"></i>
                <%= current_user['email'] %>
              </div>
            </li>
            <li class="nav-item">
              <%= link_to 'Logout', :logout, class:"btn btn-sm btn-danger" %>
            </li>
          </ul>
        <% else %>
          <ul class="navbar-nav ml-auto">
            <li class="nav-item mr-2">
              <%= link_to 'Sign In', :login, class:"btn btn-sm btn-dark" %>
            </li>
            <li class="nav-item">
              <%= link_to 'Sign Up', :signup, class:"btn btn-sm btn-dark" %>
            </li>
          </ul>
        <% end %>
      </div>
            
    </nav>